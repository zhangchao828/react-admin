export declare function navigate(
  to: string | number,
  props?: {
    replace?: boolean
    state?: any
  }
)
export {
  Redirect,
  useLocation,
  BrowserRouter,
  HashRouter,
  Link,
  NavLink,
  useParams,
} from 'react-router-dom'
