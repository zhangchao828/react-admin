{
  "npmClient": "yarn",
  "packages": ["packages/*"],
  "useWorkspaces": true,
  "version": "independent",
  "command": {
    "publish": {
      "allowBranch": ["main"],
      "registry": "https://registry.npmjs.org/"
    },
    "version": {
      "allowBranch": ["main"],
      "exact": true
    }
  }
}
